# ─────────────────────────────────────────────────────────────────
# application-example.yml
# Copy this file, fill in real values, save as application-local.yml
# (which is git-ignored) or set the equivalent environment variables.
# NEVER commit real credentials.
# ─────────────────────────────────────────────────────────────────

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stms          # DB_URL
    username: your_db_username                           # DB_USERNAME
    password: your_db_password                           # DB_PASSWORD
